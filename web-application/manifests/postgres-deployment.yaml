apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: web-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          value: webappdb
        - name: POSTGRES_USER
          value: webapp
        - name: POSTGRES_PASSWORD
          value: webapppass
        volumeMounts:
        - name: pgdata
          mountPath: /var/lib/postgresql/data
      volumes:
      - name: pgdata
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: web-app
spec:
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
